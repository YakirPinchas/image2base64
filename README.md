# Image ⇆ Base64 Converter

A simple, fast and secure online tool that converts **images to Base64** and **Base64 back to images**.  
Everything runs **locally in your browser** – your files are never uploaded to any server.

👉 Live Demo: (Add your Netlify/GitHub Pages link here)

---

## ✨ Features
- Convert image → Base64
- Convert Base64 → Image
- Drag & drop image support
- Copy to clipboard
- Download Base64 as `.txt`
- 100% client-side (privacy focused)
- Clean, responsive UI
- Works on mobile & desktop

---

## 🚀 How It Works
The tool uses the browser’s FileReader API to read the image and convert it into a DataURL.  
No backend is required.

---

## 🛠️ Technologies
- HTML5  
- CSS (custom design)  
- Vanilla JavaScript  
- FileReader API  

---

## 📦 Installation (Optional)
You can run this locally:

```bash
git clone https://github.com/<your-username>/image2base64
cd image2base64
open index.html
